# ansible_role_openjournal

Playbook para instalar un servidor de revistas openjournal via ansible en equipos con linux + notas para migración del servicio.

Testeado con qemu + vagrant + ubuntu2004 + ansible 2.10